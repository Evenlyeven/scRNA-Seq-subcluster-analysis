A basic helper pipeline for scRNA-seq subcluster analysis, customized to work with our in-house data.

Usage: `Rscript sub_analysis.R -i /path/to/input.RData -p monocyte,macrophage -o /path/to/output_dir/`
